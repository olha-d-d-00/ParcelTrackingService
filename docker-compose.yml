version: '3.9'

services:
  web:
    build: .
    container_name: parcel_tracking_service
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8002:8000"
    volumes:
      - .:/app